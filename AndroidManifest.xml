<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cris.programa.vnote"
    android:installLocation="preferExternal"
    android:versionCode="18"
    android:versionName="18.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="cris.programa.vnote.MainActivity"
            android:configChanges="locale|orientation"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="cris.programa.vnote.NoteActivity"
            android:configChanges="locale|orientation"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="cris.programa.vnote.SelectItems"
            android:configChanges="locale|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="cris.programa.vnote.AppPreferences"
            android:configChanges="locale|orientation"
            android:label="@string/app_name" />
    </application>

</manifest>